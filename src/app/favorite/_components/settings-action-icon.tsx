"use client";

import { Settings } from "lucide-react";

const SettingsActionIcon = () => {
  return (
    <>
      <Settings
        size={16}
        onClick={() => {
          // TODO: 画面遷移
        }}
      />
    </>
  );
};

export default SettingsActionIcon;
